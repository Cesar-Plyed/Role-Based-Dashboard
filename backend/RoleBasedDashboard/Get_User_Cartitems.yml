info:
  name: Get_User_Cartitems
  type: http

http:
  method: GET
  url: http://localhost:8080/api/cart/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
